# HTTP client
requests>=2.28,<3.0

# Data validation (pin to pydantic v1.x to avoid breaking API changes in v2)
pydantic>=1.10.12,<2.0

# Testing
pytest>=7.0,<8.0
pytest-html>=3.1.1

# Test helpers / fixtures
Faker>=18.9.0

# Optional: typing helpers for older Python versions (uncomment if needed)
# typing-extensions>=4.0
