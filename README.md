# Restful Booker API Automation - Pytest

## Setup

```bash
git clone <your-repo-url>
cd restful-booker-pytest

python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

pip install -r requirements.txt
